apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: koldavar-ingressroute
spec:
  entryPoints:
    - web
  routes:
    {{- range $key, $svc := .Values.services }}
    - match: PathPrefix(`/{{ $svc.name }}`)
      kind: Rule
      services:
        - name: {{ $svc.name }}-service
          port: {{ $svc.port }}
    {{- end }}
