apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redpanda
  namespace: {{ .Values.kafka.namespace }}
spec:
  serviceName: redpanda
  replicas: {{ .Values.kafka.redpanda.replicas }}
  selector:
    matchLabels:
      app: redpanda
  template:
    metadata:
      labels:
        app: redpanda
    spec:

      initContainers:
        - name: fix-permissions
          image: busybox:1.36
          command: ["sh","-c"]
          args:
            - |
              echo "Fixing data dir permissions..."
              mkdir -p /var/lib/redpanda/data
              chown -R 101:101 /var/lib/redpanda/data
              chmod -R 775 /var/lib/redpanda/data
          volumeMounts:
            - name: redpanda-storage
              mountPath: /var/lib/redpanda/data

      containers:
        - name: redpanda
          image: "{{ .Values.kafka.image.repository }}:{{ .Values.kafka.image.tag }}"
          command: ["/entrypoint.sh"]
          args:
            - redpanda
            - start
            - --overprovisioned
            - --smp={{ .Values.kafka.redpanda.smp }}
            - --memory={{ .Values.kafka.redpanda.memory }}
            - --reserve-memory={{ .Values.kafka.redpanda.reserveMemory }}
            - --kafka-addr=PLAINTEXT://0.0.0.0:9092
            - --advertise-kafka-addr=PLAINTEXT://{{ .Values.kafka.redpanda.advertisedKafkaAddr }}
            - --pandaproxy-addr=0.0.0.0:{{ .Values.kafka.redpanda.pandaproxyPort }}

          ports:
            - containerPort: 9092
            - containerPort: 9644

          volumeMounts:
            - name: redpanda-storage
              mountPath: /var/lib/redpanda/data

      volumes:
        - name: redpanda-storage
          persistentVolumeClaim:
            claimName: redpanda-data
