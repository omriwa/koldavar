apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: {{ .Values.kafka.namespace }}
spec:
  clusterIP: None
  selector:
    app: redpanda
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
    - name: admin
      port: 9644
      targetPort: 9644
