apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Values.redis.name }}
  name: {{ .Values.redis.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.redis.name }}
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: {{ .Values.redis.name }}
    spec:
      containers:
      - image: {{ .Values.redis.image }}
        name: {{ .Values.redis.name }}
        resources: {}
status: {}
