name: CI-CD

on: [push]

jobs:
  go-compiler-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - inputExtraction
          - syncManager
          - textSynthesizer

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Build service ${{ matrix.service }}
        run: bash ./scripts/check_go_services.sh ${{ matrix.service }}

  python-compiler-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - audioToText
          # - tts

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build service ${{ matrix.service }}
        run: bash ./scripts/check_python_services.sh ${{ matrix.service }}

  compiler-checks:
    runs-on: ubuntu-latest
    needs:
      - go-compiler-check
      - python-compiler-check
    steps:
      - run: echo "All compiler checks passed"
        
  docker-images:
    runs-on: ubuntu-latest
    needs:
      - compiler-checks

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Debug location
        run: |
          echo "PWD:"
          pwd
          echo "List:"
          ls -la

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push with Docker Compose
        env:
          BUILDX_BAKE_ENTITLEMENTS_FS: 0
        run: |
          cd docker
          docker buildx bake --allow=fs.read=.. -f docker-compose.yaml --push

  setup-on-remote:
    runs-on: ubuntu-latest
    needs:
      - docker-images
    
    steps:
      - name: connect
        run: |
          echo "${{ secrets.REMOTE_PRIVATE_KEY }}" > key
          echo "Trying to connect..."
          echo "address - ${{ secrets.REMOTE_ADDRESS }}, key:"
          cat key
          ssh root@${{ secrets.REMOTE_ADDRESS }} -i key "echo 'SSH connection OK. Server hostname:' && hostname"
          


